SVGMapper Minimal

This is a minimal WPF app implementing Polygon drawing, Grid/Snap, Seating, Inspector placeholder, Undo/Redo (basic) and SVG export.

Build:

```powershell
dotnet build SVGMapper.Minimal.csproj
```

Run:

```powershell
dotnet run --project SVGMapper.Minimal.csproj
```
