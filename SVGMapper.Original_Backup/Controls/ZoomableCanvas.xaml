<UserControl x:Class="SVGMapper.Controls.ZoomableCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Background="Transparent">
        <!-- Grid layer (drawn in world coordinates) -->
        <Canvas x:Name="GridCanvas" IsHitTestVisible="False" />

        <!-- Main drawing canvas (hosts shapes, tools etc.) -->
        <Canvas x:Name="ContentCanvas" Background="Transparent"
                Focusable="True" />

        <!-- Simple toolbar -->
        <StackPanel Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8">
            <Button Content="+" Width="28" Height="28" Click="ZoomIn" Margin="0,0,0,6"/>
            <Button Content="−" Width="28" Height="28" Click="ZoomOut" Margin="0,0,0,6"/>
            <ToggleButton x:Name="GridToggle" Content="Grid" IsChecked="True" Checked="GridToggle_Checked" Unchecked="GridToggle_Unchecked" Width="48" Margin="0,0,0,6"/>
            <ToggleButton x:Name="SnapToggle" Content="Snap" IsChecked="False" Checked="SnapToggle_Checked" Unchecked="SnapToggle_Unchecked" Width="48"/>
        </StackPanel>
    </Grid>
</UserControl>